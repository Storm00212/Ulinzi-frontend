import { AppRegistry } from 'react-native';
import App from './App';

// In a real React Native project, the app name would typically come from app.json
const appName = 'Ulinzi';

AppRegistry.registerComponent(appName, () => App);
